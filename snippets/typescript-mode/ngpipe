# -*- mode: snippet -*-
# name: Angular Pipe
# key: ngpipe
# --
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
  name: '$1'
})
export class ${1:$(upcase-initials yas/text)}Pipe implements PipeTransform {
  transform(input: any): string {
    $0
  }
}
